export type DetectiveProfile={version:1;nickname:string;avatar:{baseCharacter:string;skinTone:string;hairstyle:string;top:string;bottom:string;hat:string;shoes:string;badge:string;backpack:string}};export const DETECTIVE_PROFILE_KEY="ip2kids.detective-profile";export function readDetectiveProfile():DetectiveProfile|null{try{const raw=window.localStorage.getItem(DETECTIVE_PROFILE_KEY);const data=raw?JSON.parse(raw):null;return data?.version===1&&data.nickname&&data.avatar?data:null}catch{return null}}export function saveDetectiveProfile(profile:DetectiveProfile){try{window.localStorage.setItem(DETECTIVE_PROFILE_KEY,JSON.stringify(profile));return true}catch{return false}}